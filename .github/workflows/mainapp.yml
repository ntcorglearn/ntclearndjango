name: Django CI

on:
  push:
    branches: [ "manozdevops" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: self-hosted
    steps:
    - name: Hello world check
      run: |
        echo "hello world"
        hostname

